trigger:
- '*'

variables:
  buildConfiguration: 'Release'

stages:
- stage: 'Build'
  displayName: 'Build the web application'
  jobs: 
  - job: 'Build'
    displayName: 'Build job'
    pool: vs23822_AgentPool

    variables:
      wwwrootDir: 'Tailspin.SpaceGame.Web/wwwroot'
      dotnetSdkVersion: '5.x'

    steps:
    - task: UseDotNet@2
      displayName: 'Use .NET SDK $(dotnetSdkVersion)'
      inputs:
        version: '$(dotnetSdkVersion)'

    - task: Npm@1
      displayName: 'Run npm install'
      inputs:
        verbose: false

    - script: './node_modules/.bin/node-sass $(wwwrootDir) --output $(wwwrootDir)'
      displayName: 'Compile Sass assets'

    - task: gulp@1
      displayName: 'Run gulp tasks'

    - script: 'echo "$(Build.DefinitionName), $(Build.BuildId), $(Build.BuildNumber)" > buildinfo.txt'
      displayName: 'Write build info'
      workingDirectory: $(wwwrootDir)

    - task: DotNetCoreCLI@2
      displayName: 'Restore project dependencies'
      inputs:
        command: 'restore'
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Build the project - $(buildConfiguration)'
      inputs:
        command: 'build'
        arguments: '--no-restore --configuration $(buildConfiguration)'
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Publish the project - $(buildConfiguration)'
      inputs:
        command: 'publish'
        projects: '**/*.csproj'
        publishWebProjects: false
        arguments: '--no-build --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)/$(buildConfiguration)'
        zipAfterPublish: true

    - publish: '$(Build.ArtifactStagingDirectory)'
      artifact: drop

- stage: 'Deploy_Azure_dev'
  displayName: 'Deploy the web application to Azure'
  dependsOn: Build
  jobs:
  - deployment: AzureAppDeploy_dev
    pool: vs23822_AgentPool
    environment: dev
    variables:
    - group: Release
    strategy:
      runOnce:
        deploy:
          steps:
          - download: current
            artifact: drop
          - task: AzureWebApp@1
            displayName: 'Azure App Service Deploy: website'
            inputs:
              azureSubscription: 'Resource Manager - Tailspin - Space Game'
              #Library:Variable Group::Release:::WebAppName=tailspin-space-game-web-deploy
              appName: '$(WebAppName)'
              package: '$(Pipeline.Workspace)/drop/$(buildConfiguration)/*.zip'

- stage: 'Deploy_vs23822_dev'
  displayName: 'Deploy the web application to vs23822'
  dependsOn: Build
  jobs:
  - deployment: VMDeploy_dev
    pool: vs23822_AgentPool
    environment:
        name: vs23822_dev
        resourceType: VirtualMachine
        tags: vs23822,dev
    strategy:
      runOnce:
        deploy:
          steps:
#          - download: current
#            artifact: drop          

#          - task: PublishPipelineArtifact@1
#            inputs:
#              targetPath: /home/bernhard/azure.devops/stages/dev
#              artifactName: WebApp

#          - task: DownloadBuildArtifacts@1
#            inputs:
#              buildtype: current
#              buildVersionToDownload: latest
#              artifactName: DownloadBuildArtifacts
#              downloadPath: '$(System.ArtifactsDirectory)' 

          - task: DownloadPipelineArtifact@1
            inputs:
              source: 'current'
              appName: '$(WebAppName)'
              package: '$(Pipeline.Workspace)/drop/$(buildConfiguration)/*.zip'

#          - task: CopyFiles@2
#            inputs:
#              sourceFolder: '$(Build.SourcesDirectory)'
#              contents: '**/$(BuildConfiguration)/**/?(*.exe|*.dll|*.pdb)'
#              targetFolder: '$(Build.ArtifactStagingDirectory)'

          - task: PublishBuildArtifacts@1
            inputs:
              pathToPublish: '$(Build.ArtifactStagingDirectory)'
              artifactName: CopyFiles

